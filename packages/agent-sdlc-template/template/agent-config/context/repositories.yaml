# repositories.yaml
# This file serves as the canonical map of repositories for the organization.
# Use this to guide agents on where different domains and projects live.

repositories:
  - name: "YourGithubOrg/example-service"
    linear_team: "EXP"
    description: "The primary backend service."
    domain: "Backend"

  - name: "YourGithubOrg/example-web"
    linear_team: "WEB"
    description: "The frontend web application."
    domain: "Frontend"
